import Routes from "Routes";
import { Provider } from 'react-redux';
function App() {
  return (
     // @ts-ignore
    // <Provider store={store}>
      <Routes />
    // </Provider>
  );
}

export default App;
